const connection = require('../database/connection');
const checkSchedule = require('../utilities/checksSchedule')
const Moment = require('moment')


module.exports = {
  async correctionRequest(request, response){
    
  },

  async correctionAccept(request, response){

  },

  async correctionReject(request, response){

  },

  async correctionUpdate(request, response){

  },

  async correctionList(request, response){

  },

  async correctionSearch(request, response){

  }

}